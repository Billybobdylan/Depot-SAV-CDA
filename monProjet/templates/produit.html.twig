{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}

<div class="card">
      <div class="row">
    
         {% for p in produit.produit %}
         <div class="col-12 col-sm-6 col-md-4 col-lg-3 col-xl-2 ">
                        <div class="bordure-grise px-3 pt-3 mb-3 shadow">
                        <img class="img1" src='/images/illu{{ p.id }}.jpg'/>
                             <a href="/categorie/sous/.../{{ p.id }}">
                                <h4 class="text-center">
                                {{ p.libelle }}
                               </a>
                                </h4> 
                                {{ p.prix }}
                            
                        </div>
                    </div>

          
                       
                   
                {% endfor %}
        </div>
    </div>
      

{% endblock %}